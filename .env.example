# Copy this file to .env on the Synology (next to docker-compose.yml)
# and fill in the real value. Never commit the real .env to git.

# Cloudflare Tunnel token: Zero Trust dashboard -> Networks -> Tunnels ->
# create a tunnel -> copy the token from the install command.
TUNNEL_TOKEN=paste-the-tunnel-token-here
